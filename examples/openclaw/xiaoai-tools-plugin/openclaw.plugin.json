{
  "id": "xiaoai-tools",
  "name": "XiaoAI Tools",
  "description": "Optional toolset that lets the xiaoai agent control the local XiaoAI bridge.",
  "version": "0.1.0",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "enabled": {
        "type": "boolean",
        "default": true,
        "description": "Enable or disable registration of XiaoAI tools."
      },
      "bridgeBaseUrl": {
        "type": "string",
        "minLength": 1,
        "default": "http://127.0.0.1:4400",
        "description": "Base URL for the local XiaoAI bridge debug API."
      },
      "timeoutMs": {
        "type": "integer",
        "minimum": 1000,
        "maximum": 60000,
        "default": 10000,
        "description": "HTTP timeout for bridge tool calls in milliseconds."
      }
    }
  }
}
